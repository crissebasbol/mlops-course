[project]
name = "jupyter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cristhian Bolanos", email = "crissebasbol@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
    "numpy",
    "seaborn",
    "scikit-learn",
]

[project.scripts]
jupyter = "jupyter:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
